import { colors } from "./colors"

export const priorityMap = {
  HIGH: { label: "Alta", color: colors.highPriority },
  MEDIUM: { label: "Média", color: colors.mediumPriority },
  LOW: { label: "Baixa", color: colors.lowPriority }
}
